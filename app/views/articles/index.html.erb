  <div class="list-group">
    <% @articles.each do |article| %>
        <div class="d-flex w-100 justify-content-between mt-2">
          <h5 class="mb-1"><%= link_to article.title, article_path(article) %></h5>
        </div>
          <p class="mb-1"><%= article.content %></small>
      </a>
    <% end %>
  </div>
